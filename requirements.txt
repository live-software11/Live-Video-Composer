# Live Video Composer - Dipendenze
# Installa con: pip install -r requirements.txt

# Elaborazione immagini (obbligatorio) - Python 3.9+ richiesto per Pillow 12
Pillow>=12.1.0

# Elaborazione video (opzionale ma consigliato per export MP4/GIF)
# opencv-python-headless: stessa API di opencv-python, ~50MB in meno, nessuna dipendenza Qt
opencv-python-headless>=4.10.0

# Supporto array per video (richiesto se opencv-python-headless installato)
numpy>=1.26.0

# Drag & drop nativo Windows (obbligatorio per D&D)
windnd>=1.0.7

# Fallback D&D se windnd non disponibile (opzionale)
tkinterdnd2>=0.4.3

# Build eseguibile (solo per sviluppo: pip install pyinstaller)
# pyinstaller>=6.0
